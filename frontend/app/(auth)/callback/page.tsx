"use client";

// This is a placeholder for handling authentication callbacks
// from providers like OAuth (Google, etc.) in the future.

const CallbackPage = () => {
  return (
    <div className="flex items-center justify-center min-h-screen">
      <p>Processing login...</p>
    </div>
  );
};

export default CallbackPage;
